//! Compatibility shim for older `timeseries_table_core::helpers::time_bucket::*` paths.
//!
//! The canonical implementation lives in [`crate::coverage::bucket`].

pub use crate::coverage::bucket::*;
