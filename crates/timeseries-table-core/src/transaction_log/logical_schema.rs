//! Compatibility shim for older `timeseries_table_core::transaction_log::logical_schema::*`
//! paths.
//!
//! The canonical implementation lives in [`crate::metadata::logical_schema`].

pub use crate::metadata::logical_schema::*;
