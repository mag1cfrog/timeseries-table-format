//! Compatibility shim for older `timeseries_table_core::common::time_column::*` paths.
//!
//! The canonical implementation lives in [`crate::metadata::time_column`].

pub use crate::metadata::time_column::*;
