# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Local data downloads
data/
!examples/data/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Python
**/__pycache__/
**/*.py[cod]
.pytest_cache/
.venv/

# Python packaging artifacts
build/
dist/
**/*.egg-info/

# maturin wheel output (note: `target/` is already ignored above)
target/wheels/

# Built Python extension modules (keep scoped to Python package dirs)
python/**/*.so
python/**/*.pyd
python/**/*.dylib

# Local Cargo config (developer-specific)
.cargo/config.toml

# direnv (developer-specific)
.envrc
.direnv/
